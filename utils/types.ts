export type UserData = {
  uid: string
  name: string
  joined: number
  email: string
}

export type NoteData = {
  date: string
  emoji: string
  note: string
  created: number
  complete: boolean
}
